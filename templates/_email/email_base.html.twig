{% apply inline_css(encore_entry_css_source('email')) %}
<!doctype html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width">
    <title>{%- block title -%}{{ kbin_meta_title() }}{%- endblock -%}</title>
</head>
<body> 
    <div class="header container">
        <a href="{{absolute_url(path('root'))}}">
             <img class="logo" width="50" height="50" src="https://www.theytree.com/static/images/logo.png" alt="{{ kbin_title() }}">
         </a>
    </div>
    <div class="body container">
        {% block body %}{% endblock %}
    </div>
    <div class="footer container">
         <h3>© {{ 'now'|date('Y') }} {{ kbin_domain() }}</h3>
    </div>
</body>
</html>
{% endapply %} 